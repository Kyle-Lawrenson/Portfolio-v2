const Home = (index) => {
  return (
    <div key={index} className="title-container">
      <h1 className="title">Kyle Lawrenson's</h1>
      <h2 className="subtitle">Portfolio</h2>
    </div>
  );
};

export default Home;
